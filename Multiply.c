#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    /* get value from user */
    long long int A, B;
    scanf("%lld %lld", &A, &B);
    
    // print the multiplication
    printf("%lld", A * B);
    return 0;
}
